---
phase: 04-mock-interview-mode
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [server/src/models/InterviewSession.js, server/src/routes/sessions.js, server/src/index.js, server/src/routes/dashboard.js, server/src/scripts/test-mock-session.js]
autonomous: true
requirements: [MOCK-01, MOCK-03]
must_haves:
  truths:
    - "InterviewSession model defines schema with user, role, questions, answers status and start details"
    - "sessionsRouter registers POST /start, PUT /:id/save, and POST /:id/submit endpoints"
    - "sessionsRouter is mounted in index.js"
    - "dashboard.js routes query and return count of active mock sessions instead of 0 placeholder"
  artifacts:
    - "server/src/models/InterviewSession.js"
    - "server/src/routes/sessions.js"
    - "server/src/scripts/test-mock-session.js"
  key_links: []
---

<objective>
Implement the database model, API routers, and test scripts to manage mock interview rounds, saving progress and grading answers using the AI evaluation pipeline.

Purpose: Provide the backend business logic and storage layer for mock interviews.
Output: Integrated session database and routes.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mock-interview-mode/04-CONTEXT.md
@.planning/phases/04-mock-interview-mode/04-RESEARCH.md
@.planning/phases/04-mock-interview-mode/04-VALIDATION.md
@.planning/phases/04-mock-interview-mode/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InterviewSession Mongoose Model</name>
  <files>server/src/models/InterviewSession.js</files>
  <read_first>server/src/models/User.js</read_first>
  <action>Create server/src/models/InterviewSession.js defining the InterviewSession schema with: user (ref User), role (String), questions (array of ref Question), answers (array of subdocuments matching userAnswer, scores, feedback items), status (active/completed), startedAt, completedAt, and overallScore. Export model.</action>
  <verify>node -e "require('mongoose')"</verify>
  <acceptance_criteria>
    - server/src/models/InterviewSession.js exists
    - Schema contains user, questions, answers, status, and startedAt fields
  </acceptance_criteria>
  <done>InterviewSession model created</done>
</task>

<task type="auto">
  <name>Task 2: Build sessions.js Express Router</name>
  <files>server/src/routes/sessions.js, server/src/index.js</files>
  <read_first>server/src/index.js</read_first>
  <action>Create server/src/routes/sessions.js. Implement endpoints:
  - POST /start: Accepts { role }, selects 10 questions (3 Easy, 4 Medium, 3 Hard spread across Technical, Behavioral, HR, Aptitude). Saves InterviewSession and returns it.
  - PUT /:id/save: Accepts { answers: [{ questionId, userAnswer }] }. Updates active session answers.
  - POST /:id/submit: Sets status to completed, evaluates all answers concurrently using the existing evaluateAttempt logic, averages overall scores, and returns scorecard details.
  Mount router in server/src/index.js.</action>
  <verify>node -e "console.log('Router configured')"</verify>
  <acceptance_criteria>
    - server/src/routes/sessions.js exists
    - Router defines start, save, and submit endpoint handlers
    - server/src/index.js registers sessions route mount
  </acceptance_criteria>
  <done>sessionsRouter implemented and mounted</done>
</task>

<task type="auto">
  <name>Task 3: Integrate activeSessions count in stats API</name>
  <files>server/src/routes/dashboard.js</files>
  <read_first>server/src/routes/dashboard.js</read_first>
  <action>Open server/src/routes/dashboard.js. Update the GET /stats handler to replace the placeholder `activeSessions: 0` with `activeSessions: await InterviewSession.countDocuments({ user: userId, status: 'active' })`.</action>
  <verify>node -e "console.log('Stats endpoint updated')"</verify>
  <acceptance_criteria>
    - server/src/routes/dashboard.js imports InterviewSession model
    - stats JSON output returns count of active sessions
  </acceptance_criteria>
  <done>Active sessions count integrated in dashboard statistics</done>
</task>

<task type="auto">
  <name>Task 4: Create automated test script test-mock-session.js</name>
  <files>server/src/scripts/test-mock-session.js</files>
  <read_first>server/src/scripts/test-dashboard.js</read_first>
  <action>Create server/src/scripts/test-mock-session.js. Connect to mongoose, find/create a test user, invoke sessions start, assert 10 questions distribution (3 easy, 4 medium, 3 hard), test save answer, submit session, and assert completed status with grading score output. Exit with code 0 on success.</action>
  <verify>node -e "console.log('Script scaffolded')"</verify>
  <acceptance_criteria>
    - server/src/scripts/test-mock-session.js exists
  </acceptance_criteria>
  <done>Automated verification script created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `node server/src/scripts/test-mock-session.js` and check database session logs
</verification>

<success_criteria>
- InterviewSession database creation succeeds
- API endpoints start, save, and submit return correct responses
- Dashboard stats endpoint includes real activeSessions counts
- Verification script passes clean assertions
</success_criteria>

<output>
After completion, create `.planning/phases/04-mock-interview-mode/04-01-SUMMARY.md`
</output>
