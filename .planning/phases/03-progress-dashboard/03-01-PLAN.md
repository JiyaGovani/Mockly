---
phase: 03-progress-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [server/src/index.js, client/package.json, client/src/App.jsx, server/src/routes/dashboard.js, client/src/pages/Dashboard.jsx, server/src/scripts/test-dashboard.js]
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "recharts library is installed in client package.json"
    - "Dashboard backend route /api/dashboard/stats returns stats filtered by range query"
    - "Dashboard page displays responsive LineChart and RadarChart using Recharts"
  artifacts:
    - "server/src/routes/dashboard.js"
    - "client/src/pages/Dashboard.jsx"
    - "server/src/scripts/test-dashboard.js"
  key_links: []
---

<objective>
Build backend API routes to aggregate user practice metrics and timeseries data, install recharts on the client, and construct the frontend Dashboard layout.

Purpose: Provide students with clear visual analytics representing their learning progress and strengths/weaknesses.
Output: Integrated progress tracker dashboard page.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-progress-dashboard/03-CONTEXT.md
@.planning/phases/03-progress-dashboard/03-RESEARCH.md
@.planning/phases/03-progress-dashboard/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts dependency and set up test-dashboard helper script</name>
  <files>client/package.json, server/src/scripts/test-dashboard.js</files>
  <read_first>client/package.json</read_first>
  <action>Install the 'recharts' library in the client package.json. Create server/src/scripts/test-dashboard.js to serve as the verification script that connects to MongoDB, seeds dummy practice attempts for a test user if needed, and checks the stats calculations.</action>
  <verify>node -e "console.log('Wave 0 ready')"</verify>
  <acceptance_criteria>
    - client/package.json contains 'recharts'
    - test-dashboard.js exists
  </acceptance_criteria>
  <done>Recharts installed and validation script scaffolded</done>
</task>

<task type="auto">
  <name>Task 2: Build backend stats aggregation API endpoint</name>
  <files>server/src/routes/dashboard.js, server/src/index.js</files>
  <read_first>server/src/index.js</read_first>
  <action>Create server/src/routes/dashboard.js. Implement GET /api/dashboard/stats route that takes optional 'range' parameter (7days, 30days, all). Filter user attempts by range, and aggregate metrics: total questions practiced, average score, and chart series (chronological line chart attempts grouped by type, and average score grouped by type for radar chart). Register dashboard router in server/src/index.js.</action>
  <verify>node server/src/scripts/test-dashboard.js --stats</verify>
  <acceptance_criteria>
    - server/src/routes/dashboard.js exists
    - GET /api/dashboard/stats handles range query parameter and returns correct aggregate metrics
  </acceptance_criteria>
  <done>Backend dashboard aggregation API completed</done>
</task>

<task type="auto">
  <name>Task 3: Create frontend Dashboard page with line and radar charts</name>
  <files>client/src/pages/Dashboard.jsx, client/src/App.jsx</files>
  <read_first>client/src/App.jsx</read_first>
  <action>Create client/src/pages/Dashboard.jsx. Fetch user stats from /api/dashboard/stats. Render three statistics cards at the top (Total Questions, Average Score, Active Sessions placeholder). Render a LineChart showing chronological attempt scores for SDE question types (technical, behavioral, hr, aptitude), and a RadarChart representing average score mastery per topic. Add route and navigation link in App.jsx.</action>
  <verify>node -e "import('fs').then(() => console.log('Dashboard UI built'))"</verify>
  <acceptance_criteria>
    - client/src/pages/Dashboard.jsx imports LineChart and RadarChart from 'recharts'
    - client/src/pages/Dashboard.jsx fetches from /api/dashboard/stats
  </acceptance_criteria>
  <done>Progress Dashboard UI and routing integrated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `node server/src/scripts/test-dashboard.js --stats` and check the JSON outputs
</verification>

<success_criteria>
- Recharts installed and loaded without errors on client
- Backend stats API returns correct JSON averages and attempts
- Dashboard page renders widgets and charts correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-progress-dashboard/03-01-SUMMARY.md`
</output>
