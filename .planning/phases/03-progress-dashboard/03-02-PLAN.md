---
phase: 03-progress-dashboard
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [server/src/routes/dashboard.js, client/src/pages/Dashboard.jsx, server/src/scripts/test-dashboard.js]
autonomous: true
requirements: [REC-01]
must_haves:
  truths:
    - "Backend endpoint /api/dashboard/recommendations returns 3 recommended questions"
    - "Weakest topics prioritizes unattempted topics first, then lowest average score topics"
    - "Suggested questions are unattempted questions of user's weakest topics matching user's role"
  artifacts: []
  key_links:
    - "Dashboard.jsx displays recommendation cards linking to practice workspace"
---

<objective>
Build a personalized question recommendation engine on the backend, and integrate recommendations cards onto the student dashboard page.

Purpose: Guide students dynamically to practice topics where they need improvement.
Output: Question recommendation panel on the dashboard.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-progress-dashboard/03-CONTEXT.md
@.planning/phases/03-progress-dashboard/03-RESEARCH.md
@.planning/phases/03-progress-dashboard/03-VALIDATION.md
@.planning/phases/03-progress-dashboard/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build question recommendation logic and API route</name>
  <files>server/src/routes/dashboard.js, server/src/scripts/test-dashboard.js</files>
  <read_first>server/src/routes/dashboard.js</read_first>
  <action>Extend server/src/routes/dashboard.js to add GET /api/dashboard/recommendations route. The recommendation engine should: 1) Retrieve all question types. 2) Check user's attempt history to find weakest topics (unattempted topics first, then lowest average score topics). 3) For the top 3 weakest topics, retrieve 1 question matching user's role that has NOT been attempted. Fallback to lowest scored question if all are attempted. Return list of 3 questions.</action>
  <verify>node server/src/scripts/test-dashboard.js --recommendations</verify>
  <acceptance_criteria>
    - GET /api/dashboard/recommendations returns 3 question objects
    - Recommendations prioritize unattempted topics
    - Returned questions match user's SDE/DS/PM role
  </acceptance_criteria>
  <done>Recommendation engine backend route completed</done>
</task>

<task type="auto">
  <name>Task 2: Integrate recommendations cards on the frontend Dashboard</name>
  <files>client/src/pages/Dashboard.jsx</files>
  <read_first>client/src/pages/Dashboard.jsx</read_first>
  <action>Modify client/src/pages/Dashboard.jsx to fetch suggested questions from /api/dashboard/recommendations. Render a 'Recommended Questions' section displaying these questions as interactive cards, showing topic, difficulty, and a 'Practice Now' link that redirects the user to the practice workspace '/practice/:id'.</action>
  <verify>node -e "console.log('UI recommendations integrated')"</verify>
  <acceptance_criteria>
    - Dashboard page renders recommendation cards
    - Cards display question details and link to practice workspace
  </acceptance_criteria>
  <done>Recommendations section added to Dashboard UI</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `node server/src/scripts/test-dashboard.js --recommendations` and check recommendation output
</verification>

<success_criteria>
- Recommendations API correctly outputs SDE/DS/PM questions matching weak topic criteria
- Recommended question cards display on client dashboard and correctly link to practice pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-progress-dashboard/03-02-SUMMARY.md`
</output>
