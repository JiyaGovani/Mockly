---
phase: 02-core-ai-evaluation
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [server/src/services/llmService.js, server/src/services/evaluationCoordinator.js, server/src/routes/evaluation.js, server/src/index.js, server/src/models/Question.js]
autonomous: true
requirements: [EVAL-03, EVAL-04]
must_haves:
  truths:
    - "LLM service calls local Ollama llama3 model to evaluate answers"
    - "Evaluation coordinator correctly blends scores: 0.25 keyword, 0.35 embedding, 0.40 LLM"
    - "API route /api/evaluation/submit processes submissions and returns scores/feedback"
  artifacts:
    - "server/src/services/llmService.js"
    - "server/src/services/evaluationCoordinator.js"
    - "server/src/routes/evaluation.js"
  key_links:
    - "evaluationCoordinator.js calls keywordService, embeddingService, and llmService"
---

<objective>
Build LLM grading service, prompt templates, overall evaluation coordinator, and answer submission API endpoint.

Purpose: Provide a unified backend pipeline that rates, scores, and provides structured feedback on user submissions.
Output: Evaluation backend routes and controller pipeline.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ai-evaluation/02-CONTEXT.md
@.planning/phases/02-core-ai-evaluation/02-RESEARCH.md
@.planning/phases/02-core-ai-evaluation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LLM Grading Service with structured prompts</name>
  <files>server/src/services/llmService.js</files>
  <read_first>server/src/config/ollama.js</read_first>
  <action>Create server/src/services/llmService.js. Implement the LLM call using Ollama's POST /api/generate with Llama 3 and format: json. Design structured prompts containing the question, expected answer, and user answer, asking the LLM to return a score (0-100) and structured comments (strengths, weaknesses, suggestions).</action>
  <verify>node -e "import('./server/src/services/llmService.js')"</verify>
  <acceptance_criteria>
    - llmService.js contains '/api/generate'
    - llmService.js contains 'llama3'
  </acceptance_criteria>
  <done>LLM service and evaluation prompt built</done>
</task>

<task type="auto">
  <name>Task 2: Build Evaluation Coordinator and submission API route</name>
  <files>server/src/services/evaluationCoordinator.js, server/src/routes/evaluation.js, server/src/index.js, server/src/models/Question.js</files>
  <read_first>server/src/models/Question.js</read_first>
  <action>Update Question schema to support storing expectedAnswerEmbedding. Build evaluationCoordinator.js to calculate individual scores (keyword, embedding, LLM) and blend them with weights (0.25 keyword + 0.35 embedding + 0.40 LLM). Create Express route /api/evaluation/submit to receive question ID and user's text answer, execute coordination, and return evaluation results.</action>
  <verify>node -e "import('./server/src/services/evaluationCoordinator.js')"</verify>
  <acceptance_criteria>
    - evaluationCoordinator.js contains '0.25'
    - evaluationCoordinator.js contains '0.35'
    - evaluationCoordinator.js contains '0.40'
    - evaluation.js contains '/submit'
  </acceptance_criteria>
  <done>Evaluation coordinator and API submission router built</done>
</task>

<task type="auto">
  <name>Task 3: Create automated verification test script</name>
  <files>server/src/scripts/test-eval.js</files>
  <read_first>server/src/scripts/seed.js</read_first>
  <action>Create server/src/scripts/test-eval.js to test evaluation pipeline locally. The script should seed/fetch a question, run evaluationCoordinator with a mock answer, and display score breakdown and LLM feedback details.</action>
  <verify>node server/src/scripts/test-eval.js</verify>
  <acceptance_criteria>
    - test-eval.js exists
    - test-eval.js imports evaluationCoordinator
  </acceptance_criteria>
  <done>Automated verification script created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `node server/src/scripts/test-eval.js` and verify it produces expected JSON output from local Ollama
</verification>

<success_criteria>
- Submission API `/api/evaluation/submit` is fully operational
- Automated test script `test-eval.js` executes and returns successful evaluation
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ai-evaluation/02-02-SUMMARY.md`
</output>
