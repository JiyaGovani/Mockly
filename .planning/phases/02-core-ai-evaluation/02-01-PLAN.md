---
phase: 02-core-ai-evaluation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [server/package.json, server/src/config/ollama.js, server/src/services/keywordService.js, server/src/services/embeddingService.js]
autonomous: true
requirements: [EVAL-01, EVAL-02]
must_haves:
  truths:
    - "Ollama API client is configured with configurable URL and timeout"
    - "Keyword matching service uses stemming via the natural library"
    - "Embedding service computes cosine similarity between answers"
  artifacts:
    - "server/src/config/ollama.js"
    - "server/src/services/keywordService.js"
    - "server/src/services/embeddingService.js"
  key_links: []
---

<objective>
Configure Ollama client connection, implement keyword matching using stemming, and implement cosine similarity embedding comparisons.

Purpose: Set up the core foundation for text analysis and similarity scoring.
Output: Configured services for local AI evaluation.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ai-evaluation/02-CONTEXT.md
@.planning/phases/02-core-ai-evaluation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Ollama connection and install natural library</name>
  <files>server/package.json, server/src/config/ollama.js</files>
  <read_first>server/package.json</read_first>
  <action>Install the 'natural' library in server workspace. Create server/src/config/ollama.js which exports the configured Ollama URL (default http://localhost:11434), model name (default llama3), and timeout settings (default 30000ms), all read from process.env.</action>
  <verify>node -e "import('./server/src/config/ollama.js').then(o => console.log('Ollama config loaded'))"</verify>
  <acceptance_criteria>
    - server/package.json contains '"natural"'
    - server/src/config/ollama.js contains 'localhost:11434'
  </acceptance_criteria>
  <done>Ollama configuration module and npm dependencies created</done>
</task>

<task type="auto">
  <name>Task 2: Build Keyword Service with Natural Stemming</name>
  <files>server/src/services/keywordService.js</files>
  <read_first>server/src/services/keywordService.js</read_first>
  <action>Implement keyword extraction and matching using PorterStemmer and WordTokenizer from 'natural'. Stem the user's answer tokens and match against the stems of keyPoints to correctly match plural/singular forms and verb variations.</action>
  <verify>node -e "import('./server/src/services/keywordService.js')"</verify>
  <acceptance_criteria>
    - keywordService.js contains 'PorterStemmer'
    - keywordService.js contains 'matchKeyPoints'
  </acceptance_criteria>
  <done>Keyword matching service built</done>
</task>

<task type="auto">
  <name>Task 3: Build Embedding Service with Cosine Similarity</name>
  <files>server/src/services/embeddingService.js</files>
  <read_first>server/src/services/embeddingService.js</read_first>
  <action>Implement the embedding service which queries Ollama's POST /api/embeddings using axios and the configured model (nomic-embed-text). Implement a helper to calculate the cosine similarity between the expected answer embedding and user answer embedding.</action>
  <verify>node -e "import('./server/src/services/embeddingService.js')"</verify>
  <acceptance_criteria>
    - embeddingService.js contains 'calculateCosineSimilarity'
    - embeddingService.js contains '/api/embeddings'
  </acceptance_criteria>
  <done>Embedding similarity service built</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Ensure natural and axios modules load correctly in the server environment
</verification>

<success_criteria>
- Natural library installed
- Ollama configuration file exported
- Keyword stemming and cosine similarity services return valid outputs
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ai-evaluation/02-01-SUMMARY.md`
</output>
