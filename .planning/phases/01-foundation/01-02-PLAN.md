---
phase: 01-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [server/src/models/User.js, server/src/controllers/authController.js, server/src/routes/auth.js, client/src/context/AuthContext.jsx, client/src/pages/Login.jsx, client/src/pages/Register.jsx]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Users can sign up and log in via email and password"
    - "JWT tokens are generated, stored on client, and verified on page reload"
  artifacts:
    - "server/src/models/User.js"
    - "server/src/controllers/authController.js"
    - "client/src/context/AuthContext.jsx"
  key_links:
    - "AuthContext.jsx calls authController.js registration/login routes"
---

<objective>
Verify User model, Auth controllers/routes, and register/login forms.

Purpose: Confirm user authentication functions properly on both client and server.
Output: Verified JWT-based user authentication flow.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify server User model and auth controllers</name>
  <files>server/src/models/User.js, server/src/controllers/authController.js, server/src/routes/auth.js</files>
  <read_first>server/src/models/User.js</read_first>
  <action>Verify that User model hashes passwords before saving using bcryptjs and has comparePassword. Verify that register/login/me controllers generate JWT and verify credentials.</action>
  <verify>node -e "import('./server/src/models/User.js').then(() => console.log('User model loaded'))"</verify>
  <acceptance_criteria>
    - User.js contains 'comparePassword'
    - authController.js contains 'generateToken'
  </acceptance_criteria>
  <done>Server authentication models and controllers verified</done>
</task>

<task type="auto">
  <name>Task 2: Verify client AuthContext and Register/Login UI</name>
  <files>client/src/context/AuthContext.jsx, client/src/pages/Login.jsx, client/src/pages/Register.jsx</files>
  <read_first>client/src/context/AuthContext.jsx</read_first>
  <action>Verify that AuthProvider restores session on startup via /auth/me route and provides login/register forms with loading and error states.</action>
  <verify>node -e "import('fs').then(() => console.log('Auth files exist'))"</verify>
  <acceptance_criteria>
    - AuthContext.jsx contains 'localStorage.getItem(\'mockly_token\')'
    - Login.jsx contains 'login'
    - Register.jsx contains 'register'
  </acceptance_criteria>
  <done>Client authentication UI and state management verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Ensure Mongoose schema maps correctly to MongoDB
</verification>

<success_criteria>
- User registration and login flow works correctly
- Tokens persist in localStorage and are sent as Bearer headers
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
